we can use both generator as well iterrator to return value on demand only diff is in generator we use fuction to return value if ask
and in iterrator we use iter(iterableObject) to return value if ask or demand.

The purpose of using Both have same that reduce memory space in main memory.

Generator:
    use if you have to use any fuction as module
Iterator:
    use if you have to use value on demand on same file.